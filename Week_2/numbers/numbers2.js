let price = 19.56789;
console.log(price.toFixed(2));

let twoDecimals = price.toFixed(2)
console.log('Price with two decimals: '+ twoDecimals);

let noDecimal = price.toFixed(0);
console.log('Price with no decimals: '+ noDecimal);

console.log (`Price with no decimals: ${noDecimal}`);

